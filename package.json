{
  "name": "mechanic-incentive",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd backend && npm run build",
    "start": "cd backend && node dist/app.js",
    "dev": "cd frontend && npm run dev"
  }
}
